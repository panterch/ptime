#menu
  %h2 Places
  %ul
    %li{:class => "#{activeWhenController('entries')}"}
      != link_to('Entries', { :controller => 'entries' } )
    -if @current_user.is_admin? 
      %li{:class => "#{activeWhenController('projects')}"}
        != link_to 'Projects', projects_path
      %li{:class => "#{activeWhenController('users')}"}
        != link_to 'Users', users_path
    %li
      != link_to 'Logout', { :controller => 'login', :action => 'logout' }

