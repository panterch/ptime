%h2.grid_12 Milestone types

.box.grid_12
  %h2=link_to 'Milestone Management', '#', :class => 'toggle-box'
  %div
    %table.milestone_types
      %thead
        %tr
          %th Name
          %th Edit

      - @milestone_types.each do |milestone_type|
        %tr
          %td= milestone_type.name
          %td= link_to (image_tag 'edit.png'),
            edit_milestone_type_path(milestone_type)
